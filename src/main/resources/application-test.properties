spring.application.name=api
spring.datasource.url=jdbc:mysql://localhost/vollmed_api_test?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456&

# esto es para que consultas  query sql se estan haciendo
# desde la aplicaion a la base de datos
#spring.jpa.show-sql=true
# para dar un mejor formato mas legible
#spring.jpa.properties.hibernate.format_sql=true
#
#
#
#server.error.include-stacktrace=never
#
#api.security.secret=${JWT_SECRET:123456}
#spring.devtools.restart.enabled=true


# IMOPORTANTE  es para usar una base datos en memoria no externa como mysql la que estamos usando arriba
# seberia eliminar esta anotacion @AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
# de nuesta clase MedicoRepositoryTest

## spring.datasource.url=jdbc:h2:mem:testdb

# usuario por defecto es sa y password poe defecto es password

##spring.datasource.driverClassName=org.h2.Driver
## spring.datasource.username=sa
##spring.datasource.password=password
## spring.jpa.database-platform=org.hibernate.dialect.H2Dialect